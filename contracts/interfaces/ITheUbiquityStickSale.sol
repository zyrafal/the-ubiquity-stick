// SPDX-License-Identifier: MIT
pragma solidity ^0.8.9;

interface ITheUbiquityStickSale {
  ////////////////////////////////////////////////////////////////////////////////////////////////////
  /// @notice EVENTS
  ////////////////////////////////////////////////////////////////////////////////////////////////////
  event Mint(address from, uint256 count, uint256 price);

  event Payback(address to, uint256 unspent);

  event Withdraw(address to, address token, uint256 amount);

  ////////////////////////////////////////////////////////////////////////////////////////////////////
  /// @notice PAY FUNCTIONS
  ////////////////////////////////////////////////////////////////////////////////////////////////////
  receive() external payable;

  function withdraw() external;

  ////////////////////////////////////////////////////////////////////////////////////////////////////
  /// @notice GETTER
  ////////////////////////////////////////////////////////////////////////////////////////////////////
  function allowance(address addr) external returns (uint256 count, uint256 price);

  ////////////////////////////////////////////////////////////////////////////////////////////////////
  /// @notice SETTERS
  ////////////////////////////////////////////////////////////////////////////////////////////////////
  function setTokenContract(address newTokenContract) external;

  function setFundsAddress(address addr) external;

  function batchSetAllowances(
    address[] calldata addresses,
    uint256[] calldata counts,
    uint256[] calldata prices
  ) external;

  function setAllowance(
    address addr,
    uint256 count,
    uint256 price
  ) external;
}
